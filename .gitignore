# Environment and cache
.env              # 환경 변수 파일 → 보안용, 깃허브에 올리면 안 됨
instance/         # SQLite DB 등 Flask 기본 인스턴스 폴더
__pycache__/      # 파이썬 캐시 폴더 → 자동 생성, 필요 없음
*.pyc             # 컴파일된 파이썬 파일 → 필요 없음